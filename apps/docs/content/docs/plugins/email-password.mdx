---
title: Email and Password
description: Core email/password authentication.
icon: Mail
---

```python
from kernia.plugins.email_password import email_and_password

plugins=[email_and_password()]
```

Routes:

```http
POST /sign-up/email
POST /sign-in/email
POST /sign-out
GET  /get-session
POST /forget-password
POST /reset-password
```

Use this plugin for normal account creation, login, logout, and password reset.
