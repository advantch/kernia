[project]
name = "kernia-api-key"
version = "0.0.0"
description = "API key plugin for Kernia"
requires-python = ">=3.11"
dependencies = [
    "kernia",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kernia_api_key"]
