[project]
name = "kernia-scim"
version = "0.0.0"
description = "SCIM 2.0 provisioning plugin"
requires-python = ">=3.11"
dependencies = [
    "kernia",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kernia_scim"]
